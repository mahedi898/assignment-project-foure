function mindGame(a) {
    const multiply = a * 3;
    const plus = multiply + 10;
    const devided = plus / 2;
    const oddIs = devided - 5;
    return oddIs;
  }
  const outputs = 5;
  console.log(mindGame(outputs));